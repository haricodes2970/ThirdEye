{
  "name": "thirdeye-chrome-extension",
  "version": "1.0.2",
  "private": true,
  "scripts": {
    "build:bg": "vite build --config vite.config.js",
    "build:popup": "vite build --config vite.config.popup.js",
    "build": "npm run build:bg && npm run build:popup"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.4"
  },
  "devDependencies": {
    "vite": "^6.2.3"
  }
}
